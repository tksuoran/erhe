[
 {
  "path": "test_assets/ColorSpaceTests/UsdPreviewSurface/usduvtexture_color_test.usda",
  "diagnostics": [
   "Attribute `inputs:sourceColorSpace`: `lin_ap1_scene` is not an allowed token",
   "USD image '.*\\.hdr' could not be decoded"
  ],
  "appearance": "the test's own README states that Hydra cannot honor colorSpace metadata on the file input, colorSpace:name on the input, or colorSpace:name on an enclosing scope; erhe follows sourceColorSpace only, as Storm does",
  "reason": "column 5 authors the illegal sourceColorSpace token `lin_ap1_scene` on purpose; LightUSD warns and keeps the fallback `auto`, which is what OpenUSD does with it (asset README); the Radiance .hdr textures of rows 1 and 3 are not decoded - a known limitation tracked in doc/usd-compatibility-plan.md section 6, not a by-design result"
 },
 {
  "path": "test_assets/foundation/stage_composition/inherit_and_specialize.usda",
  "diagnostics": [
   "inherits from '.*', which is not a class prim - it becomes no style"
  ],
  "reason": "the inherits and specializes arcs target a def Cube, and both inheriting prims author every inherited opinion locally (displayColor, translate), so the arcs change nothing visible; composing them is future work (doc/usd-compatibility-plan.md section 6); Storm match 0.95"
 },
 {
  "path": "test_assets/foundation/stage_composition/payload/payload_invalid.usda",
  "diagnostics": [
   "Prefab source file not found",
   "failed to load reference target"
  ],
  "reason": "the file payloads a file that does not exist, on purpose (README: 'invalid file'); OpenUSD reports the unresolved arc and composes nothing for it, and so does erhe"
 },
 {
  "path": "test_assets/foundation/stage_composition/references/reference_invalid.usda",
  "diagnostics": [
   "Prefab source file not found",
   "failed to load reference target"
  ],
  "reason": "the file references a file that does not exist, on purpose (README: 'invalid file'); OpenUSD reports the unresolved arc and composes nothing for it, and so does erhe"
 },
 {
  "path": "test_assets/foundation/stage_composition/references_prim/reference_prim_in_other_file.usda",
  "diagnostics": [
   "Prefab '.*': prim '.*' is not in",
   "Prefab source file not found",
   "failed to load reference target",
   "produced no nodes - not caching"
  ],
  "reason": "two of the three prim references name a prim or a file that does not exist, on purpose (README); the existing one composes and the other two are reported unresolved, as OpenUSD does"
 },
 {
  "path": "test_assets/foundation/stage_composition/references_prim/reference_prim_in_same_file.usda",
  "diagnostics": [
   "Prefab '.*': prim '.*' is not in",
   "failed to load reference target",
   "produced no nodes - not caching"
  ],
  "reason": "both internal prim references name prims that do not exist: `Cube_with_invalid_reference` on purpose (README), and `Cube_with_reference` by an asset defect - it references </World/cube> while the file defines `Cube` (paths are case-sensitive); OpenUSD resolves neither and renders one cube, as erhe does (Storm match 0.98)"
 },
 {
  "path": "test_assets/foundation/stage_composition/subLayer/sublayer_invalid.usda",
  "diagnostics": [
   "not found in path",
   "Composite subLayers failed"
  ],
  "reason": "the file sublayers a file that does not exist, on purpose (README: 'invalid file'); OpenUSD reports the unresolved layer and composes nothing for it, and so does erhe"
 },
 {
  "path": "test_assets/foundation/stage_configuration/upAxis/upAxis_X.usda",
  "diagnostics": [
   "is not a valid upAxis \\(Y or Z\\) - imported as Y-up"
  ],
  "reason": "the file authors an upAxis value USD does not allow, on purpose (README: 'one invalid value'); Hydra applies no up-axis transform to the geometry and usdview reads the token as Y, which is what erhe renders too (Storm match 0.94)"
 },
 {
  "path": "test_assets/foundation/stage_configuration/upAxis/upAxis_invalid.usda",
  "diagnostics": [
   "is not a valid upAxis \\(Y or Z\\) - imported as Y-up",
   "Ignore unknown .* value\\. Must be"
  ],
  "reason": "the file authors an upAxis value USD does not allow, on purpose (README: 'one invalid value'); Hydra applies no up-axis transform to the geometry and usdview reads the token as Y, which is what erhe renders too (Storm match 0.94)"
 },
 {
  "path": "test_assets/schemaTests/usdGeom/extent/scaled_extent.usda",
  "gaps": [
   "bounds are off the composed stage's"
  ],
  "reason": "both cubes author a wrong `extent` on purpose (0.1 and 1.0 for 0.5 cubes, README 'Scaled Extent'); UsdGeomBBoxCache trusts the authored extent, so the composed bounds are the wrong ones while erhe bounds the actual points; Storm match 0.83"
 },
 {
  "path": "test_assets/schemaTests/usdGeom/meshes/empty_mesh/empty.usda",
  "gaps": [
   "no mesh loaded"
  ],
  "reason": "the file is one Mesh prim with no attributes at all; its README states the render is empty as expected"
 },
 {
  "path": "test_assets/schemaTests/usdGeom/meshes/normals_types/normalsTypes.usda",
  "diagnostics": [
   "Attribute `normals` declared with a non-conformant type"
  ],
  "reason": "a parser robustness test: sixteen of its seventeen meshes declare `normals` with a type the schema does not give it, on purpose (README); LightUSD reports each and keeps the value as a custom property, and all 17 meshes load and render"
 },
 {
  "path": "test_assets/schemaTests/usdGeom/meshes/points_types/pointsTypes.usda",
  "diagnostics": [
   "Attribute `points` declared with a non-conformant type"
  ],
  "gaps": [
   "of the .* composed meshes loaded",
   "bounds are off the composed stage's"
  ],
  "reason": "a parser robustness test: twelve of its sixteen meshes declare `points` with a type no renderer can read as points, on purpose (README); Storm renders the four Vec3f-typed ones and so does erhe (Storm match 0.84), while the composed count and bounds take in the other twelve Mesh prims and their authored extents"
 },
 {
  "path": "test_assets/TextureTransformTest/TextureTransformTest.usd",
  "diagnostics": [
   "Primvar `tangents` has no authored value"
  ],
  "reason": "every one of the twelve meshes authors `float4[] primvars:tangents = []` - an empty array left behind by the glTF conversion (the glTF source carries no TANGENT attribute); an empty primvar has no data to convert, so LightUSD's Tydra skips it with this warning (a deliberate OpenUSD-compat path in render-data-mesh.cc whose comment names this very asset), and OpenUSD skips it just as silently - Storm renders the same frame (storm_match 0.99, bounds_deviation 0.0)"
 },
 {
  "path": "full_assets/CarbonFrameBike/CarbonFrameBike.usdz",
  "diagnostics": [
   "Failed to get texture coordinate for `st`"
  ],
  "reason": "of the asset's 331 Mesh prims exactly one, </CarbonFrameBike/Geom/CarbonFrameBike/Bike_v2/Rahmen/Rahmen_3/Federachse/ZB_RobertS_2016/inafag_na4902_2rsr/inafag_na4902_2rsr_4891_4/Shape_4380>, authors no `primvars:st` (measured with pxr); its bound material </CarbonFrameBike/Materials/Material__37> is a constant-color UsdPreviewSurface with no connected input, which erhe imports with every texture slot empty, so the missing UV set changes nothing it draws - Tydra asks for `st` all the same and repeats the resulting line, which carries no prim path and an empty reason, in one accumulated warning batch; erhe imports 331 of 331 meshes and 31 of 31 materials and matches the Storm render on the objects it draws (0.92)"
 },
 {
  "path": "full_assets/ElephantWithMonochord/SoC-ElephantWithMonochord.usdc",
  "diagnostics": [
   "Skipping empty `blendShape.*` declaration in SkelAnimation Prim",
   "Skipping animated attribute 'extent'",
   "TODO: Prim type SpatialAudio"
  ],
  "reason": "the SkelAnimation prim </SoC_ElephantWithMonochord/Object/Elefant/_anim> declares `blendShapes` and `blendShapeWeights` with no authored value and no time sample (measured with pxr), which OpenUSD reads as no blend shapes and reports nothing about; `extent` is a bound rather than something drawn, and the asset's own README ('Known Issue: Bounds calculations') states its extents may be wrong and that a viewer may compute new bounds from the skinned data instead, which is what erhe does with the 2540 samples Tydra skips; and the README states the file carries UsdAudio data on purpose, which erhe keeps as a Typed prim without its schema attributes (doc/usd-compatibility-plan.md section 5, the Typed prim of unsupported type). erhe imports 3 of 3 meshes and 2 of 2 materials and matches the Storm render on the objects it draws (0.99)"
 },
 {
  "path": "full_assets/McUsd/McUsd.usda",
  "diagnostics": [
   "DomeLight texture '.*' is not sampled"
  ],
  "reason": "the file authors a textured DomeLight; erhe has no environment map, so the dome contributes the constant radiance of its color, intensity and exposure only and names the texture in this warning - a known limitation tracked in doc/usd-compatibility-plan.md section 6 (an environment map from a DomeLight texture), not a by-design result. Everything the entry draws is right: 23 of 23 meshes, 23 of 23 materials, bounds_deviation 0.0, and the by-eye note records the opaque blocks matching the reference with the stained-glass cube see-through and both cards two-sided"
 },
 {
  "path": "full_assets/McUsd/McUsd.usdz",
  "diagnostics": [
   "DomeLight texture '.*' is not sampled"
  ],
  "reason": "the file authors a textured DomeLight; erhe has no environment map, so the dome contributes the constant radiance of its color, intensity and exposure only and names the texture in this warning - a known limitation tracked in doc/usd-compatibility-plan.md section 6 (an environment map from a DomeLight texture), not a by-design result. Everything the entry draws is right: 23 of 23 meshes, 23 of 23 materials, bounds_deviation 0.0, and the by-eye note records the opaque blocks matching the reference with the stained-glass cube see-through and both cards two-sided"
 },
 {
  "path": "full_assets/McUsd/McUsd_10cm.usda",
  "diagnostics": [
   "DomeLight texture '.*' is not sampled"
  ],
  "reason": "the file authors a textured DomeLight; erhe has no environment map, so the dome contributes the constant radiance of its color, intensity and exposure only and names the texture in this warning - a known limitation tracked in doc/usd-compatibility-plan.md section 6 (an environment map from a DomeLight texture), not a by-design result. Everything the entry draws is right: 23 of 23 meshes, 23 of 23 materials, bounds_deviation 0.0, and the by-eye note records the opaque blocks matching the reference with the stained-glass cube see-through and both cards two-sided"
 },
 {
  "path": "full_assets/McUsd/McUsd_10cm.usdz",
  "diagnostics": [
   "DomeLight texture '.*' is not sampled"
  ],
  "reason": "the file authors a textured DomeLight; erhe has no environment map, so the dome contributes the constant radiance of its color, intensity and exposure only and names the texture in this warning - a known limitation tracked in doc/usd-compatibility-plan.md section 6 (an environment map from a DomeLight texture), not a by-design result. Everything the entry draws is right: 23 of 23 meshes, 23 of 23 materials, bounds_deviation 0.0, and the by-eye note records the opaque blocks matching the reference with the stained-glass cube see-through and both cards two-sided"
 },
 {
  "path": "full_assets/StandardShaderBall/standard_shader_ball_scene.usda",
  "diagnostics": [
   "TODO: Prim type NodeGraph",
   "the referencing layer defines typed prims over the reference",
   "authors .* opinion\\(s\\) that erhe has no place for",
   "USD image '.*\\.exr' could not be decoded",
   "binds material '.*', which the file has no prim for"
  ],
  "gaps": [
   "of the 12 composed meshes loaded"
  ],
  "reason": "every line this entry reports names a decision the plan already states. The composed stage holds twelve meshes (measured with pxr); erhe loads ten because the three under </standard_shader_ball_scene/shader_ball/neutral_objects> are defined over a referencing prim - a structural edit inside a reference, which section 5 puts out of scope and which the warning names - while the hoisted second `surface_geometry` variant adds one inactive `material_surface`. The four materials erhe does not convert (13 of 17) are the MaterialX ones under </standard_shader_ball_scene/materials/examples/mtlx>, whose surface is an `ND_open_pbr_surface_surfaceshader` network the importer does not read (section 6, MaterialX), which is why the four variant bindings to them are dropped and why two `NodeGraph` prims stay Typed prims without their schema attributes (section 5). The variant opinions a variant set has no place for are the section 6 variants item, and the six undecoded neutral maps are `.exr`, which section 6's image-decoding item names. bounds_deviation 0.0 and the lights and cameras all compose"
 },
 {
  "path": "full_assets/OpenChessSet/chess_set.usda",
  "diagnostics": [
   "a MaterialX document is not a USD layer",
   "binds '.*' to material '.*', which the file has no prim for"
  ],
  "reason": "every material of this asset is a MaterialX document referenced as a layer (`Chessboard_mat.mtlx` and its six siblings), which the editor refuses as a reference target, so none of the 41 composed materials arrives and the `shadingVariant` bindings that name them are dropped with it - the MaterialX item of doc/usd-compatibility-plan.md section 6, a known limitation rather than a by-design result; the .mtlx reader is behind LIGHTUSD_WITH_USDMTLX, off in erhe's build. The geometry is right: bounds_deviation 0.0, and the pieces render in the default grey record the null-material fallback supplies",
  "gaps": [
   "renders in the unbound default"
  ],
  "appearance": "the by-eye note records every mesh in the unbound default grey where the reference shows dark and light stone with green and gold accents; that is what a file whose materials are all MaterialX documents looks like until the MaterialX item of doc/usd-compatibility-plan.md section 6 is taken up - the pieces themselves stand where the reference puts them"
 },
 {
  "path": "full_assets/SubdivisionSurfaces/Creases_SpinningPyramids.usda",
  "diagnostics": [
   "authors .* opinion\\(s\\) that erhe has no place for",
   "must be `.*` for UsdUVTexture",
   "Failed to convert UsdPreviewSurface",
   "has no converted material - it is not placed in the tree",
   "the referencing layer defines typed prims over the reference",
   "binds '.*' to material '.*', which the file has no prim for",
   "hole face removal is only applied when triangulation is enabled"
  ],
  "reason": "each pyramid's material connects `inputs:diffuseColor` to a `UsdPrimvarReader_float3` reading `displayColor` (Pyramid/mtl.usda), and Tydra accepts only a `UsdUVTexture` output on a shader input, so it fails the material and erhe imports 0 of 3 - a known limitation with its own item in doc/usd-compatibility-plan.md section 6, not a by-design result, and the reason the two variant bindings that name those materials are dropped as well. The `mtl` prims the referencing layer defines over the reference are a structural edit inside a reference, which section 5 puts out of scope. The uncarried variant opinions are the section 6 variants item. The hole-face line is Tydra reporting that it removes hole faces only when it triangulates, which the conversion does not ask it to; erhe drops the four hole facets itself, so the pyramids show the openings Storm shows (24 facets per shape, from 28 before the fix) and bounds_deviation measures 0.0"
 },
 {
  "path": "full_assets/Teapot/Teapot.usd",
  "diagnostics": [
   "authors .* opinion\\(s\\) that erhe has no place for",
   "must be `.*` for UsdUVTexture",
   "Failed to convert UsdPreviewSurface",
   "TODO: Prim type NodeGraph",
   "the referencing layer defines typed prims over the reference"
  ],
  "gaps": [
   "no mesh loaded"
  ],
  "reason": "the model's geometry is composed entirely inside a variant block: Teapot.usd payloads Teapot_Payload.usd, whose sublayer Teapot_Materials.usd references Teapot_Geometry.usd, whose `Utah` and `Fancy` variants each author the reference to the geometry on the variant itself, and whose `Materials` scope is a def authored inside a variant block over that reference. erhe's hoist carries a variant's def children and property opinions but not the arcs they author, and the section 5 reference-structure rule drops a def authored over a reference, so nothing composes - a known limitation with its own item in doc/usd-compatibility-plan.md section 6, not a by-design result. The material lines behind it are the same file's UsdPreviewSurface reading a primvar through a connection Tydra refuses (the UsdPrimvarReader item of section 6); erhe imports 0 of the 1 composed mesh and 0 of the 2 materials"
 },
 {
  "path": "full_assets/Teapot/DrawModes.usd",
  "diagnostics": [
   "authors .* opinion\\(s\\) that erhe has no place for",
   "must be `.*` for UsdUVTexture",
   "Failed to convert UsdPreviewSurface",
   "TODO: Prim type NodeGraph",
   "the referencing layer defines typed prims over the reference",
   "the typeless prim has unreadable xformOps"
  ],
  "gaps": [
   "no mesh loaded"
  ],
  "reason": "the model's geometry is composed entirely inside a variant block: Teapot.usd payloads Teapot_Payload.usd, whose sublayer Teapot_Materials.usd references Teapot_Geometry.usd, whose `Utah` and `Fancy` variants each author the reference to the geometry on the variant itself, and whose `Materials` scope is a def authored inside a variant block over that reference. erhe's hoist carries a variant's def children and property opinions but not the arcs they author, and the section 5 reference-structure rule drops a def authored over a reference, so nothing composes - a known limitation with its own item in doc/usd-compatibility-plan.md section 6, not a by-design result. The material lines behind it are the same file's UsdPreviewSurface reading a primvar through a connection Tydra refuses (the UsdPrimvarReader item of section 6); this file references that model once per draw mode, so it imports 0 of 35 meshes and 0 of 70 materials. Its own extra line is the duplicates' xformOp stack: `/World/FancyTeapot_1` and its siblings give `xformOpOrder` the two ops `[\"xformOp:transform\", \"xformOp:transform:duplicate1\"]` while authoring only the second, because the first arrives through their internal reference to `/World/FancyTeapot_0`; the stack is reconstructed from a prim's own properties, so it reads as unreadable and the prim keeps the composed transform - the second of the two section 6 items, also a known limitation"
 },
 {
  "path": "full_assets/Vehicles/USD_Mini_Car_Kit/assets/vehicles/vehicleVariants.usda",
  "diagnostics": [
   "Main loop STALLED",
   "breadcrumb t=",
   "Failed to read property 'elementType'",
   "does not exist in Prim .*\\(GeomSubset\\)"
  ],
  "gaps": [
   "Main loop STALLED"
  ],
  "reason": "the kit's variant sets are hoisted whole, so a stage the composer reads as 91 prims and 6 meshes arrives as 2373 prims and 146 meshes, and the load takes long enough for the main-loop watchdog to report the tick stuck in `raytrace: BVH commit` and print its breadcrumbs - the load-performance item of doc/usd-compatibility-plan.md section 6, which names that scan as the first of its three causes; a known limitation, not a by-design result. The elementType lines are Tydra's animation scan asking for a builtin that the binary 4wdGeo.usd leaves unauthored on its GeomSubsets (its .usda siblings author `uniform token elementType = \"face\"`, which is also USD's fallback), and nothing animatable rides that attribute: every subset converts, giving the entry its 432 materials. The geometry itself is right - the bounds deviate by 0.02 from the composed stage, and the Storm render matches at 0.86"
 },
 {
  "path": "intent-vfx/scenes/simpleAssetScene.usd",
  "diagnostics": [
   "the referencing layer defines typed prims over the reference",
   "which names no material - the binding is dropped",
   "Main loop STALLED",
   "breadcrumb t="
  ],
  "gaps": [
   "Main loop STALLED"
  ],
  "reason": "the scene rings 2000 copies of simpleAsset around the origin, natively instanced and through 28 PointInstancers, and arrives as 9862 prims and 2000 meshes that take 92 s to settle, long enough for the main-loop watchdog to report the tick stuck in `raytrace: BVH commit` and print its breadcrumbs - the load-performance item of doc/usd-compatibility-plan.md section 6, which now names these scenes; a known limitation, not a by-design result. The geometry is right: the bounds match the composed stage exactly (deviation 0.0) and the Storm render of the same view matches at 0.95. The material lines are the asset's own structure meeting the first item of section 5, which puts structural edits inside a reference out of scope: `assets/simpleAsset/mtl.usd` authors `prepend references = @./geo.usd@` on `/simpleAsset` and defines the `mtl` scope holding both materials as a child of that same referencing prim, so erhe drops the scope and then the bindings that name it, and the proxy cubes render with the default material. pxr reads the composed binding as `/__Prototype_1/mtl/proxy_material`, whose UsdPreviewSurface carries `inputs:diffuseColor = (0.1077, 0.9012, 0.0382)`; the reference render shows the same grey erhe does (every pixel of logs/usd_wg_survey/storm/intent-vfx_scenes_simpleAssetScene.usd.png is neutral), so nothing but that green is lost here"
 },
 {
  "path": "intent-vfx/scenes/teapotScene.usd",
  "diagnostics": [
   "has protoIndex .* out of range",
   "the referencing layer defines typed prims over the reference",
   "does not exist in Prim .*\\(GeomSubset\\)",
   "names no item of the instance",
   "Main loop STALLED",
   "breadcrumb t="
  ],
  "gaps": [
   "Main loop STALLED"
  ],
  "reason": "the scene rings 945 teapots around the origin through 23 PointInstancers and arrives as 8535 prims, which take 16 s to settle and the whole load long enough for the main-loop watchdog to report the tick stuck and print its breadcrumbs - the load-performance item of doc/usd-compatibility-plan.md section 6, which names these scenes. The protoIndex lines are the sublayer relationship item of the same section: `teapotScene_layoutOverrides.usd` prepends the three coloured prototypes to each instancer's `prototypes` and `teapotScene_layout.usd` prepends the single plain one, and LightUSD's sublayer merge admits a weaker opinion only when it is stored as a path vector, so pxr reads four targets on `/Scene/ring008/instancer_teapot008` (`red_teapot, green_teapot, blue_teapot, teapot`, measured with UsdGeom.PointInstancer) where erhe reads three and skips every instance whose `protoIndices` entry is 3, 2816 of them. The material lines are the first item of section 5, which puts structural edits inside a reference out of scope: `assets/teapot/mtl.usd` authors `prepend references = @./geo.usd@` on `/teapot` and defines the `mtl` scope holding the material as a child of that same referencing prim, so erhe drops the scope, and the teapots that do arrive render with the default material where Storm renders them red, green, blue and grey. The override line is that same binding, whose path crosses a second reference inside the instance (the nested-override item of section 6). The GeomSubset lines are Tydra's animation scan (`render-data-anim.cc` `ExtractPrimPropertyAnimation`) failing to read `indices`, which carries nothing animatable, the way the vehicle kit's subsets fail on `elementType`; the mesh converts whole, measured over MCP on the asset alone: 2472 facets and 1286 vertices in one primitive. The placement is right - the bounds match the composed stage exactly"
 },
 {
  "path": "intent-vfx/scenes/teapotScene_layout.usd",
  "diagnostics": [
   "the referencing layer defines typed prims over the reference",
   "does not exist in Prim .*\\(GeomSubset\\)",
   "names no item of the instance",
   "Main loop STALLED",
   "breadcrumb t="
  ],
  "gaps": [
   "Main loop STALLED"
  ],
  "reason": "the layout layer alone rings 1000 teapots around the origin through 23 PointInstancers and arrives as 8808 prims that take 101 s to settle, long enough for the main-loop watchdog to report the tick stuck and print its breadcrumbs - the load-performance item of doc/usd-compatibility-plan.md section 6, which names these scenes. The bounds match the composed stage exactly and the Storm render of the same view matches at 0.91. The material lines are the first item of section 5, which puts structural edits inside a reference out of scope: `assets/teapot/mtl.usd` authors `prepend references = @./geo.usd@` on `/teapot` and defines the `mtl` scope holding the material as a child of that same referencing prim, so erhe drops the scope and the teapots render with the default material. The override line is that same layer's `over \"geo\" { over \"default\" { over \"Body\" } }`, which binds the dropped material and whose path crosses a second reference inside the instance - the nested-override item of section 6. The GeomSubset lines are Tydra's animation scan (`render-data-anim.cc` `ExtractPrimPropertyAnimation`) failing to read `indices`, which carries nothing animatable, the way the vehicle kit's subsets fail on `elementType`; the mesh converts whole, measured over MCP on the asset alone: 2472 facets and 1286 vertices in one primitive"
 },
 {
  "path": "intent-vfx/scenes/teapotScene_layoutOverrides.usd",
  "diagnostics": [
   "which names no prim of the file",
   "the referencing layer defines typed prims over the reference",
   "does not exist in Prim .*\\(GeomSubset\\)",
   "names no item of the instance"
  ],
  "reason": "the file is a department override layer, not a stage of its own: `teapotScene.usd` names it in its `subLayers` list between the animation cycle and the layout, and it authors `over` opinions plus the coloured prototypes that `prepend inherits = </_class_/teapot>`. The class itself belongs to the sibling layer `teapotScene_layout.usd` (line 6260), so opened on its own the file inherits from a prim it does not hold and erhe says so once per prototype; OpenUSD composes the same file to 0 prims, 0 meshes and 0 materials, which is what the pxr leg of the survey reads. The material, GeomSubset and override lines are the teapot asset's own, named in this file's entry for `intent-vfx/scenes/teapotScene_layout.usd`"
 }
]
