#include "erhe_codegen/migration.hpp"

// Migration registry implementation is header-only (templates).
// This translation unit ensures the library has at least one .cpp file
// and can be extended with non-template migration utilities if needed.
